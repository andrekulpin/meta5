'use strict';

module.exports = function*( arr, gen, init ){
	let _init = init || 0, len = arr.length, i = 0;
	for(; i ^ len ;){
		let el = arr[ i ];
		_init = yield gen( _init, el, i++, arr );
	}
	return _init;
}